apiVersion: k8s.keycloak.org/v2alpha1
kind: KeycloakRealmImport
metadata:
  name: prd-realm-import
spec:
  keycloakCRName: ${KEYCLOAK_CR_NAME}
  realm:
    realm: prd
    enabled: true
    displayName: Production
    sslRequired: external
    registrationAllowed: false
    registrationEmailAsUsername: false
    rememberMe: false
    verifyEmail: false
    loginWithEmailAllowed: true
    duplicateEmailsAllowed: false
    resetPasswordAllowed: true
    editUsernameAllowed: false
    groups:
      - name: dagster-access
    clientScopes:
      - name: groups
        description: Group membership claim for downstream authorization
        protocol: openid-connect
        attributes:
          include.in.token.scope: "true"
          display.on.consent.screen: "false"
          gui.order: ""
        protocolMappers:
          - name: groups
            protocol: openid-connect
            protocolMapper: oidc-group-membership-mapper
            consentRequired: false
            config:
              full.path: "false"
              id.token.claim: "true"
              access.token.claim: "true"
              userinfo.token.claim: "true"
              claim.name: groups
              jsonType.label: String
    clients:
      - clientId: prd-dagster-gateway
        name: Production Dagster Gateway
        description: Production Dagster access gateway (OIDC Authorization Code + PKCE)
        enabled: true
        protocol: openid-connect
        publicClient: true
        standardFlowEnabled: true
        implicitFlowEnabled: false
        directAccessGrantsEnabled: false
        serviceAccountsEnabled: false
        redirectUris: []
        webOrigins: []
        defaultClientScopes:
          - groups
          - profile
          - email
          - roles
          - web-origins
